class Employee {
  // unsure if this is going to replace the mgr/eng/intern objects..
  constructor(name, ID, email, officeNum) {
    (this.name = name),
      (this.ID = ID),
      (this.email = email),
      (this.officeNum = officeNum);
  }
}

// module.exports = Employee;
